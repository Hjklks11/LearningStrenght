<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/cbumperro"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar"
        tools:targetApi="31">
        <activity
            android:name=".usuario.PerfilUsuarioActivity"
            android:exported="false" />
        <!-- Calculadoras -->
        <!-- Calculadora calorias -->
        <activity
            android:name=".calculadoras.calorias.CalculadoraMacrosActivity"
            android:exported="false" />
        <activity
            android:name=".calculadoras.calorias.InfoActivity"
            android:exported="false" /> <!-- Calculadora rm -->
        <activity
            android:name=".calculadoras.rm.FormulasActivity"
            android:exported="false" />
        <activity
            android:name=".calculadoras.rm.CalculadoraRmActivity"
            android:exported="false" /> <!-- Login -->
        <activity
            android:name=".login.RegisterDataActivity"
            android:exported="false" />
        <activity
            android:name=".login.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".login.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".login.MainActivity"
            android:exported="false" /> <!-- Pantalla principal -->
        <activity
            android:name=".PantallaPrincipal"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>